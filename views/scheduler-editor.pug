extends layout

block content 
  script(src='https://schedule.nylas.com/schedule-editor/v1.0/schedule-editor.js')
  .app(style='height:800px')
  // Create a button and give it an ID to reference later
  button#schedule-editor(type='button') Open Schedule Editor
    script.
      var btn = document.getElementById('schedule-editor');
      btn.addEventListener('click', function() {
      // Prompt the Schedule Editor when a user clicks on the button
      nylas.scheduler.show({
      auth: {
      // Account <ACCESS_TOKEN> with active calendar scope
      accessToken: "7ZJXdHwV3uzjRwJK7bgoyUji9ClYJV",
      },
      style: {
      // Style the Schedule Editor
      tintColor: '#32325d',
      backgroundColor: 'white',
      },
      defaults: {
      event: {
      title: '30-min Coffee Meeting',
      duration: 30,
      },
      },
      });
      });

 

